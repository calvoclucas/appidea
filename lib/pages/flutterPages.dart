import 'package:flutter/material.dart';

class FlutterPages extends StatelessWidget {
  const FlutterPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('Pages Sites', textScaleFactor: 2.0));
  }
}
