import 'package:flutter/material.dart';

class PagePlace extends StatelessWidget {
  const PagePlace({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('Pages Products', textScaleFactor: 2.0));
  }
}
